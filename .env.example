# Application
APP_HOST=0.0.0.0
APP_PORT=8000

# Batch controls
BULK_MAX_RECIPIENTS=500
BULK_SEND_DELAY_SECONDS=3
SMTP_RECONNECT_EVERY=40
SMTP_TIMEOUT_SECONDS=30
MAX_RESUME_SIZE_MB=10

# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=sainaveenadepu@gmail.com
SMTP_PASSWORD=enter your password here
SMTP_FROM_EMAIL=sainaveenadepu@gmail.com
SMTP_FROM_NAME=Sai Naveen
SMTP_STARTTLS=true
SMTP_USE_SSL=false

# Safety default: no email is transmitted while true.
# Change to false only after testing and verifying your SMTP configuration.
SMTP_DRY_RUN=false






