services:
  bulk-recruiter:
    image: sainaveenadepu/cold-email-python:latest
    container_name: bulk-recruiter
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
